export interface ProductdetailDTO{
    productId : number,
    name? : string
    category? : string,
    description? : string | null,
    isActive? : string,
    createdAt? : string | null,
    updateAt? : string | null,
    count : number
   
}